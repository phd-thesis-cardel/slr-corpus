# RQ5 -- Scopus
#
# What are the most common security misconfigurations encountered during the development of RESTful APIs?
#
# Form: corrected after the 2026-08-03 audit
# Change: A security block is now required. The original query paired configuration* with development*, both generic enough that any paper mentioning a REST API and its deployment matched; this is the query that admitted the largest share of off-topic records.
#
# Paste the block below into the TITLE-ABS-KEY field of Scopus. Line breaks are for
# reading only; the interface treats the string as one line.

TITLE-ABS-KEY("REST API" OR "RESTful API" OR "web API" OR "cloud API" OR "HTTP API" OR
"web service*" OR "microservice*" OR "API security" OR "API testing" OR "OpenAPI" OR
"service composition") AND TITLE-ABS-KEY("security misconfiguration*" OR misconfigur* OR
"insecure configuration*" OR "security configuration*" OR "hardening") AND TITLE-ABS-
KEY(securit* OR vulnerabilit* OR exposure* OR attack*) AND PUBYEAR > 1999 AND PUBYEAR <
2027 AND (DOCTYPE(ar) OR DOCTYPE(cp) OR DOCTYPE(re) OR DOCTYPE(ch)) AND
LANGUAGE(english)
