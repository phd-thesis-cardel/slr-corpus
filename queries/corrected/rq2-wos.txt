# RQ2 -- Web of Science
#
# What limitations or challenges are associated with current mutation operators?
#
# Form: corrected after the 2026-08-03 audit
# Change: Software-testing anchor added. The original third block (limitation, challenge, drawback, issue) is too generic to disambiguate the mutation homonym on its own.
#
# Paste the block below into the TS field of Web of Science. Line breaks are for
# reading only; the interface treats the string as one line.

TS=("mutation testing" OR "mutation analysis" OR "mutation operator*" OR "mutant*" OR
"mutation score" OR "mutation adequacy") AND TS=("software testing" OR "test case*" OR
"test suite*" OR "test generation" OR "testing technique*" OR "test adequacy" OR "fault
detection" OR "test data" OR "unit test*") AND TS=("REST API" OR "RESTful API" OR "web
API" OR "cloud API" OR "HTTP API" OR "web service*" OR "microservice*" OR "API security"
OR "API testing" OR "OpenAPI" OR "service composition") AND TS=(limitation* OR
challenge* OR drawback* OR issue* OR problem*) AND PY=(2000-2026)
