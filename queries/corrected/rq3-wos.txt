# RQ3 -- Web of Science
#
# What factors contribute to finding vulnerabilities in RESTful APIs?
#
# Form: corrected after the 2026-08-03 audit
# Change: Interface block widened; security terms tightened so that the generic cause/factor/source block no longer carries the query.
#
# Paste the block below into the TS field of Web of Science. Line breaks are for
# reading only; the interface treats the string as one line.

TS=("REST API" OR "RESTful API" OR "web API" OR "cloud API" OR "HTTP API" OR "web
service*" OR "microservice*" OR "API security" OR "API testing" OR "OpenAPI" OR "service
composition") AND TS=(vulnerabilit* OR "security flaw*" OR exploit* OR attack* OR
"security defect*") AND TS=(cause* OR factor* OR source* OR "root cause*" OR detect* OR
discover*) AND PY=(2000-2026)
