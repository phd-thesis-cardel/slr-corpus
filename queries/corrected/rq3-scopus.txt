# RQ3 -- Scopus
#
# What factors contribute to finding vulnerabilities in RESTful APIs?
#
# Form: corrected after the 2026-08-03 audit
# Change: Interface block widened; security terms tightened so that the generic cause/factor/source block no longer carries the query.
#
# Paste the block below into the TITLE-ABS-KEY field of Scopus. Line breaks are for
# reading only; the interface treats the string as one line.

TITLE-ABS-KEY("REST API" OR "RESTful API" OR "web API" OR "cloud API" OR "HTTP API" OR
"web service*" OR "microservice*" OR "API security" OR "API testing" OR "OpenAPI" OR
"service composition") AND TITLE-ABS-KEY(vulnerabilit* OR "security flaw*" OR exploit*
OR "attack*" OR "security defect*") AND TITLE-ABS-KEY(cause* OR factor* OR source* OR
"root cause*" OR detect* OR discover*) AND PUBYEAR > 1999 AND PUBYEAR < 2027 AND
(DOCTYPE(ar) OR DOCTYPE(cp) OR DOCTYPE(re) OR DOCTYPE(ch)) AND LANGUAGE(english)
