# RQ3 -- Web of Science
#
# What factors contribute to finding vulnerabilities in RESTful API services?
#
# This is the string as published with the review. The filters on publication
# year, document type and language were applied through the Web of Science interface at
# search time and are not part of the string; set them there.
#
# Paste the block below into the TS field. Line breaks are for reading
# only; the interface treats the string as one line.

TS=("REST* API*" OR "Cloud* API*" OR "Web API*") AND TS=("vulnerability" OR "security
flaw" OR "exploit" OR "attack") AND TS=("cause*" OR "factor*" OR "source*")
